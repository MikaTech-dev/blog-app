<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        /* --- Base Styles & Variables --- */
        :root {
            --accent-primary: #8a2be2;
            --glass-bg: rgba(255, 255, 255, 0.1);
            --glass-border: rgba(255, 255, 255, 0.18);
            --input-bg: rgba(0, 0, 0, 0.25);
            --text-primary: rgba(255, 255, 255, 0.95);
            --text-secondary: rgba(255, 255, 255, 0.7);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #1d093b 0%, #4a148c 100%);
            color: var(--text-primary);
            padding: 24px;
            padding-bottom: 80px; /* Space for fixed footer */
        }
        
        /* --- Layout Container --- */
        .container {
            display: flex;
            gap: 24px;
            max-width: 800px;
            width: 100%;
            align-items: flex-start; /* Aligns cards to the top */
        }

        /* --- Unified Card Styling --- */
        .card {
            background: var(--glass-bg);
            border-radius: 16px;
            padding: 32px;
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
        }
        
        .login-card {
            flex: 2; /* Takes up more space */
            min-width: 300px;
        }

        .info-card {
            flex: 1.5; /* Takes up less space */
        }

        /* --- Form & Info Card Content --- */
        h2, h3 {
            font-weight: 600;
            margin-bottom: 24px;
            text-align: center;
        }
        h2 { font-size: 1.8rem; }
        h3 { font-size: 1.3rem; }

        .error-message {
            color: #ffcdd2;
            margin-bottom: 20px;
            padding: 12px;
            background: rgba(229, 57, 53, 0.2);
            border-radius: 8px;
            border: 1px solid rgba(229, 57, 53, 0.4);
            font-size: 14px;
            text-align: center;
        }

        input[type="email"], input[type="password"] {
            width: 100%;
            padding: 12px;
            margin-bottom: 16px;
            border-radius: 8px;
            border: 1px solid transparent;
            background: var(--input-bg);
            color: var(--text-primary);
            outline: none;
            font-size: 15px;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }
        input[type="email"]:focus, input[type="password"]:focus {
            border-color: var(--accent-primary);
            box-shadow: 0 0 10px rgba(138, 43, 226, 0.3);
        }
        input::placeholder {
            color: rgba(255, 255, 255, 0.5);
        }

        .btn {
            width: 100%;
            padding: 12px;
            margin-top: 8px;
            background: var(--accent-primary);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            font-size: 15px;
            transition: all 0.2s ease-in-out;
        }
        .btn:hover {
            background-color: #9932cc;
            box-shadow: 0 0 15px rgba(138, 43, 226, 0.5);
            transform: translateY(-2px);
        }

        .signup-link {
            text-align: center;
            margin-top: 24px;
            font-size: 14px;
            color: var(--text-secondary);
        }
        .signup-link a {
            color: var(--text-primary);
            text-decoration: none;
            font-weight: 600;
        }
        .signup-link a:hover {
            text-decoration: underline;
        }

        .info-text p {
            font-size: 14px;
            line-height: 1.7;
            color: var(--text-secondary);
            margin-bottom: 16px;
        }
        
        /* --- Responsiveness --- */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                align-items: stretch;
            }
        }

        /* --- Fixed Footer --- */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            margin-top: 20px;
            text-align: center;
            color: var(--text-secondary);
            font-size: 14px;
            padding: 15px;
            border-top: 1px solid var(--glass-border);
            background: rgba(10, 5, 25, 0.5);
            backdrop-filter: blur(10px);
        }
    </style>
</head>
<body>
    <div class="container">
        <main class="card login-card">
            <h2>Welcome Back</h2>
            <% if (typeof error !== 'undefined') { %>
                <div class="error-message"><%= message %></div>
            <% } %>
            <form action="/auth/login" method="POST">
                <input type="email" name="email" placeholder="Email Address" required>
                <input type="password" name="password" placeholder="Password" required>
                <button type="submit" class="btn">Login</button>
            </form>
            <div class="signup-link">
                <p>Don't have an account? <a href="/auth/signup">Sign up here</a></p>
            </div>
        </main>
        
        <aside class="card info-card">
            <h3>⚠️ About This Blog</h3>
            <div class="info-text">
                <p>This is a community blog site where anyone can view the posts created by users.</p>
                <p>Feel free to explore the content and share your thoughts!</p>
                <p>If you encounter any issues or have questions, please contact me for support.</p>
            </div>
        </aside>
    </div>
    
    <footer class="footer">
        <strong>Made with ⚡ by Mikatech/Crescentech</strong>
    </footer>
</body>
</html>