<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- The title will dynamically change based on whether we are creating or editing -->
    <title><%= typeof blog === 'undefined' ? 'Create New Blog' : 'Edit Blog' %></title>
    <style>
        /* --- Base Styles & Variables --- */
        :root {
            --accent-primary: #8a2be2;
            --glass-bg: rgba(255, 255, 255, 0.1);
            --glass-border: rgba(255, 255, 255, 0.18);
            --input-bg: rgba(0, 0, 0, 0.25);
            --text-primary: rgba(255, 255, 255, 0.95);
            --text-secondary: rgba(255, 255, 255, 0.7);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #1d093b 0%, #4a148c 100%);
            color: var(--text-primary);
            padding: 24px;
            padding-bottom: 80px; /* Space for fixed footer */
        }

        /* --- Main Card Container --- */
        .card {
            width: 100%;
            max-width: 700px;
            background: var(--glass-bg);
            border-radius: 16px;
            padding: 28px;
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
        }

        .card-header {
            padding-bottom: 24px;
            margin-bottom: 24px;
            border-bottom: 1px solid var(--glass-border);
        }
        
        .back-link {
            display: inline-block;
            margin-bottom: 16px;
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 14px;
            transition: color 0.2s ease;
        }
        .back-link:hover {
            color: var(--text-primary);
            text-decoration: underline;
        }

        h2 {
            font-size: 1.8rem;
            font-weight: 600;
        }
        
        /* --- Form Styling --- */
        .form-group {
            margin-bottom: 24px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-size: 14px;
            color: var(--text-secondary);
        }

        input[type="text"], textarea, select {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid transparent;
            background: var(--input-bg);
            color: var(--text-primary);
            outline: none;
            font-size: 15px;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }
        input[type="text"]:focus, textarea:focus, select:focus {
            border-color: var(--accent-primary);
            box-shadow: 0 0 10px rgba(138, 43, 226, 0.3);
        }
        input[type="text"]::placeholder, textarea::placeholder {
            color: rgba(255, 255, 255, 0.5);
        }

        textarea {
            min-height: 250px;
            resize: vertical;
            line-height: 1.6;
        }

        .state-description {
            font-size: 13px;
            color: var(--text-secondary);
            margin-top: 10px;
            line-height: 1.5;
        }
        .state-description strong {
            color: var(--text-primary);
        }

        .form-actions {
            display: flex;
            justify-content: flex-end;
            margin-top: 28px;
        }

        .btn-submit {
            background: var(--accent-primary);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            font-size: 15px;
            transition: all 0.2s ease-in-out;
        }
        .btn-submit:hover {
            background-color: #9932cc;
            box-shadow: 0 0 15px rgba(138, 43, 226, 0.5);
            transform: translateY(-2px);
        }

        .error-message {
            color: #ffcdd2;
            margin-bottom: 20px;
            padding: 12px;
            background: rgba(229, 57, 53, 0.2);
            border-radius: 8px;
            border: 1px solid rgba(229, 57, 53, 0.4);
            font-size: 14px;
        }
        
        /* --- Fixed Footer --- */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            margin-top: 20px;
            text-align: center;
            color: var(--text-secondary);
            font-size: 14px;
            padding: 15px;
            border-top: 1px solid var(--glass-border);
            background: rgba(10, 5, 25, 0.5);
            backdrop-filter: blur(10px);
        }
    </style>
</head>
<body>
    <main class="card">
        <a href="/" class="back-link">← Back to Dashboard</a>
        <div class="card-header">
            <!-- Dynamically set the header text -->
            <h2><%= typeof blog === 'undefined' ? '📝 Create New Blog Post' : '✏️ Edit Blog Post' %></h2>
        </div>

        <% if (typeof error !== 'undefined') { %>
            <div class="error-message"><%= message %></div>
        <% } %>

        <!-- The form action and method will be set dynamically -->
        <form action="<%= typeof blog === 'undefined' ? '/blogs' : '/blogs/' + blog._id + '?_method=PUT' %>" method="POST">
            <div class="form-group">
                <label for="title">Title:</label>
                <input type="text" id="title" name="title" placeholder="Enter an engaging title" value="<%= typeof blog !== 'undefined' ? blog.title : (typeof title !== 'undefined' ? title : '') %>" required>
            </div>

            <div class="form-group">
                <label for="content">Content:</label>
                <textarea id="content" name="content" placeholder="Share your thoughts..." required><%= typeof blog !== 'undefined' ? blog.content : (typeof content !== 'undefined' ? content : '') %></textarea>
            </div>

            <div class="form-group">
                <label for="state">Blog State:</label>
                <select id="state" name="state" required>
                    <!-- Dynamically select the correct option -->
                    <option value="draft" <%= (typeof blog !== 'undefined' && blog.state === 'draft') ? 'selected' : '' %>>Draft</option>
                    <option value="published" <%= (typeof blog !== 'undefined' && blog.state === 'published') ? 'selected' : '' %>>Published</option>
                </select>
                <div class="state-description">
                    <strong>Draft:</strong> Only visible to you on your dashboard.<br>
                    <strong>Published:</strong> Visible to everyone on the main blogs page.
                </div>
            </div>

            <div class="form-actions">
                <!-- Dynamically set the button text -->
                <button type="submit" class="btn-submit">
                    <%= typeof blog === 'undefined' ? 'Create Post' : 'Update Post' %>
                </button>
            </div>
        </form>
    </main>
    
    <footer class="footer">
        <strong>Made with ⚡ by Mikatech/Crescentech</strong>
    </footer>
</body>
</html>