<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs</title>
    <style>
        /* --- Base Styles & Variables --- */
        :root {
            --accent-primary: #8a2be2; /* A vibrant purple */
            --glass-bg: rgba(255, 255, 255, 0.1);
            --glass-border: rgba(255, 255, 255, 0.18);
            --inner-glass-bg: rgba(0, 0, 0, 0.2);
            --input-bg: rgba(0, 0, 0, 0.25);
            --text-primary: rgba(255, 255, 255, 0.95);
            --text-secondary: rgba(255, 255, 255, 0.7);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            min-height: 100vh;
            background: linear-gradient(135deg, #1d093b 0%, #4a148c 100%);
            color: var(--text-primary);
            padding: 24px;
            padding-bottom: 80px; /* Add padding to prevent content from hiding behind the fixed footer */
        }

        /* --- Main Card Container --- */
        .card {
            width: 100%;
            max-width: 1000px;
            margin: 0 auto;
            background: var(--glass-bg);
            border-radius: 16px;
            padding: 28px;
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
        }

        /* --- Header --- */
        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-bottom: 24px;
            border-bottom: 1px solid var(--glass-border);
            margin-bottom: 24px;
            flex-wrap: wrap;
            gap: 16px;
        }

        .card-header h1 {
            font-size: 1.8rem;
            font-weight: 600;
        }

        .action-buttons {
            display: flex;
            gap: 12px;
            align-items: center;
        }

        .btn {
            padding: 10px 18px;
            border: 1px solid var(--glass-border);
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            font-weight: 600;
            font-size: 14px;
            transition: all 0.2s ease-in-out;
            background-color: rgba(255, 255, 255, 0.15);
            color: var(--text-primary);
        }
        .btn:hover {
            background-color: rgba(255, 255, 255, 0.25);
            transform: translateY(-2px);
        }

        .profile-icon {
            width: 38px;
            height: 38px;
            cursor: pointer;
            transition: transform 0.2s ease;
        }
        .profile-icon:hover {
            transform: scale(1.1);
        }
        .profile-icon svg path {
            fill: var(--text-primary);
        }
        
        /* --- Controls & Filters --- */
        .controls {
            background: var(--inner-glass-bg);
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 28px;
        }

        .search-box, .filter-group select {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid transparent;
            background: var(--input-bg);
            color: var(--text-primary);
            outline: none;
            font-size: 15px;
            transition: border-color 0.2s ease;
        }
        .search-box:focus, .filter-group select:focus {
            border-color: var(--accent-primary);
        }
        .search-box::placeholder {
            color: var(--text-secondary);
        }

        .filters {
            display: flex;
            gap: 20px;
            margin-top: 16px;
            flex-wrap: wrap;
        }

        .filter-group {
            flex: 1;
            min-width: 200px;
        }
        .filter-group label {
            display: block;
            margin-bottom: 8px;
            font-size: 14px;
            color: var(--text-secondary);
        }
        
        .sort-order {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .sort-order button {
            background: var(--input-bg);
            border: 1px solid transparent;
            cursor: pointer;
            color: var(--text-primary);
            padding: 12px 16px;
            border-radius: 8px;
            font-size: 14px;
            transition: background 0.2s ease;
        }
        .sort-order button:hover {
            background: rgba(255, 255, 255, 0.1);
        }
        .sort-order button.active {
            background: var(--accent-primary);
        }

        /* --- Blog List --- */
        .blog-item {
            background: var(--inner-glass-bg);
            padding: 24px;
            margin-bottom: 16px;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.08);
            transition: background 0.2s ease;
        }
        .blog-item:hover {
            background: rgba(0, 0, 0, 0.3);
        }

        .blog-title {
            color: var(--text-primary);
            margin: 0 0 10px 0;
            font-size: 1.5rem;
            font-weight: 600;
        }

        .blog-meta {
            color: var(--text-secondary);
            font-size: 13px;
            margin-bottom: 12px;
            line-height: 1.5;
        }

        .blog-content {
            color: var(--text-secondary);
            line-height: 1.7;
            font-size: 15px;
        }

        .read-more {
            display: inline-block;
            margin-top: 16px;
            color: var(--accent-primary);
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
        }
        .read-more:hover {
            text-decoration: underline;
        }
        
        /* --- Pagination & Fallback --- */
        .pagination {
            display: flex;
            justify-content: center;
            gap: 8px;
            margin-top: 28px;
        }

        .pagination a {
            padding: 8px 14px;
            border-radius: 8px;
            text-decoration: none;
            color: var(--text-primary);
            background: rgba(255, 255, 255, 0.1);
            font-size: 14px;
            transition: background 0.2s ease;
        }

        .pagination a.active {
            background: var(--accent-primary);
        }
        .pagination a:hover:not(.active) {
            background: rgba(255, 255, 255, 0.2);
        }

        .no-blogs {
            text-align: center;
            color: var(--text-secondary);
            padding: 40px;
            background: var(--inner-glass-bg);
            border-radius: 12px;
        }
        .no-blogs h2 {
            margin: 0 0 10px 0;
        }
        
        /* --- Fixed Footer --- */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            margin-top: 20px;
            text-align: center;
            color: var(--text-secondary);
            font-size: 14px;
            padding: 15px; /* Increased padding for better appearance */
            border-top: 1px solid var(--glass-border);
            background: rgba(10, 5, 25, 0.5); /* Added subtle bg for legibility */
            backdrop-filter: blur(10px); /* Added blur to match aesthetic */
        }
    </style>
</head>
<body>
    <main class="card">
        <div class="card-header">
            <h1>📚 All Blog Posts</h1>
            <div class="action-buttons">
                <a href="/dashboard" class="btn">Go to Dashboard</a>
                <a href="/auth/signup" class="profile-icon" title="Profile">
                    <svg viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg"><path d="M256,265.308c73.252,0,132.644-59.391,132.644-132.654C388.644,59.412,329.252,0,256,0 c-73.262,0-132.643,59.412-132.643,132.654C123.357,205.917,182.738,265.308,256,265.308z"/><path d="M425.874,393.104c-5.922-35.474-36-84.509-57.552-107.465c-5.829-6.212-15.948-3.628-19.504-1.427 c-27.04,16.672-58.782,26.399-92.819,26.399c-34.036,0-65.778-9.727-92.818-26.399c-3.555-2.201-13.675-4.785-19.505,1.427 c-21.55,22.956-51.628,71.991-57.551,107.465C71.573,480.444,164.877,512,256,512C347.123,512,440.427,480.444,425.874,393.104z"/></svg>
                </a>
                <a href="/auth/logout" class="profile-icon" title="Profile">
                    <svg fill="#000000" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52" enable-background="new 0 0 52 52" xml:space="preserve">
                        <g>
                            <path d="M21,48.5v-3c0-0.8-0.7-1.5-1.5-1.5h-10C8.7,44,8,43.3,8,42.5v-33C8,8.7,8.7,8,9.5,8h10
                                C20.3,8,21,7.3,21,6.5v-3C21,2.7,20.3,2,19.5,2H6C3.8,2,2,3.8,2,6v40c0,2.2,1.8,4,4,4h13.5C20.3,50,21,49.3,21,48.5z"/>
                            <path d="M49.6,27c0.6-0.6,0.6-1.5,0-2.1L36.1,11.4c-0.6-0.6-1.5-0.6-2.1,0l-2.1,2.1c-0.6,0.6-0.6,1.5,0,2.1l5.6,5.6
                                c0.6,0.6,0.2,1.7-0.7,1.7H15.5c-0.8,0-1.5,0.6-1.5,1.4v3c0,0.8,0.7,1.6,1.5,1.6h21.2c0.9,0,1.3,1.1,0.7,1.7l-5.6,5.6
                                c-0.6,0.6-0.6,1.5,0,2.1l2.1,2.1c0.6,0.6,1.5,0.6,2.1,0L49.6,27z"/>
                        </g>
                    </svg>
                </a>
            </div>
        </div>

        <div class="controls">
            <form id="searchForm" method="GET" action="/">
                <input type="text" name="search" class="search-box" placeholder="Search blogs by title or author..." value="<%= search %>">
                
                <div class="filters">
                    <div class="filter-group">
                        <label for="filter">Filter by State:</label>
                        <select name="filter" id="filter" onchange="this.form.submit()">
                            <% Object.entries(filterOptions).forEach(([value, label]) => { %>
                                <option value="<%= value %>" <%= filter === value ? 'selected' : '' %>><%= label %></option>
                            <% }); %>
                        </select>
                    </div>

                    <div class="filter-group">
                        <label for="sort">Sort by:</label>
                        <div class="sort-order">
                            <select name="sort" id="sort" onchange="this.form.submit()">
                                <% Object.entries(sortOptions).forEach(([value, label]) => { %>
                                    <option value="<%= value %>" <%= sort === value ? 'selected' : '' %>><%= label %></option>
                                <% }); %>
                            </select>
                            <input type="hidden" name="order" value="<%= order === 1 ? 'asc' : 'desc' %>">
                            <button type="button" onclick="toggleOrder()" class="<%= order === 1 ? 'active' : '' %>" title="Toggle Sort Order">
                                <%= order === 1 ? '⬆️' : '⬇️' %>
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </div>

        <div class="blog-list">
            <% if (blogs && blogs.length > 0) { %>
                <% blogs.forEach(blog => { %>
                    <div class="blog-item">
                        <h2 class="blog-title"><%= blog.title %></h2>
                        <div class="blog-meta">
                            By <%= blog.author.first_name %> <%= blog.author.last_name %> • 
                            <%= new Date(blog.createdAt).toLocaleDateString() %> • 
                            <%= blog.readCount %> <%= blog.readCount === 1 ? "read" : "reads" %> •
                            <%= blog.readingTime %> min read
                        </div>
                        <div class="blog-content">
                            <%= blog.content.substring(0, 200) %>...
                        </div>
                        <a href="/blogs/<%= blog._id %>" class="read-more">Read More →</a>
                    </div>
                <% }); %>

                <div class="pagination">
                    <% if (pagination.page > 1) { %>
                        <a href="?page=<%= pagination.page - 1 %>&search=<%= search %>&sort=<%= sort %>&order=<%= order === 1 ? 'asc' : 'desc' %>&filter=<%= filter %>">Previous</a>
                    <% } %>
                    
                    <% for(let i = 1; i <= pagination.totalPages; i++) { %>
                        <a href="?page=<%= i %>&search=<%= search %>&sort=<%= sort %>&order=<%= order === 1 ? 'asc' : 'desc' %>&filter=<%= filter %>"
                           class="<%= pagination.page === i ? 'active' : '' %>">
                            <%= i %>
                        </a>
                    <% } %>

                    <% if (pagination.page < pagination.totalPages) { %>
                        <a href="?page=<%= pagination.page + 1 %>&search=<%= search %>&sort=<%= sort %>&order=<%= order === 1 ? 'asc' : 'desc' %>&filter=<%= filter %>">Next</a>
                    <% } %>
                </div>
            <% } else { %>
                <div class="no-blogs">
                    <h2>No Blogs Found</h2>
                    <p>Try adjusting your search or filter settings.</p>
                </div>
            <% } %>
        </div>
    </main>
    
    <footer class="footer">
        <strong>Made with ⚡ by Mikatech/Crescentech</strong>
    </footer>

    <script>
        function toggleOrder() {
            const form = document.getElementById('searchForm');
            const orderInput = form.querySelector('input[name="order"]');
            orderInput.value = orderInput.value === 'asc' ? 'desc' : 'asc';
            form.submit();
        }

        // Add debounce to search input to avoid excessive requests
        let searchTimeout;
        document.querySelector('input[name="search"]').addEventListener('input', function(e) {
            clearTimeout(searchTimeout);
            searchTimeout = setTimeout(() => {
                this.form.submit();
            }, 500); // Wait 500ms after user stops typing
        });
    </script>
</body>
</html>